server:
  port: 88
  address: 0.0.0.0
spring:
  redis:
    host: 127.0.0.1
    port: 9003  # 确认所有服务都显式配置了此端口，而非默认6379
    password:    # 若有密码需填写
    lettuce:
      pool:
        max-active: 16
        max-idle: 8
        min-idle: 4
  application:
    name: gulimall-gateway
  management:
    endpoints:
      web:
        exposure:
          include: "*"
    endpoint:
      gateway:
        enabled: true

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        ip: 127.0.0.1
    gateway:
      discovery:
        locator:
          enabled: false  # 开启自动根据微服务名路由
      routes:
        - id: product_route
          uri: lb://gulimall-product
          predicates:
            - Path=/api/product/**
          filters:
            - StripPrefix=1

        - id: order_route
          uri: lb://gulimall-order
          predicates:
            - Path=/api/order/**
          filters:
            - StripPrefix=1

        - id: ware_route
          uri: lb://gulimall-ware
          predicates:
            - Path=/api/ware/**
          filters:
            - StripPrefix=1

        - id: user_route
          uri: lb://gulimall-user
          predicates:
            - Path=/api/user/**
          filters:
            - StripPrefix=1
